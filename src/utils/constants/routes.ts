type Routes = {
  HOME: string;
  PRODUCT: string;
  CART: string;
  CHECKOUT: string;
  ORDER_SUMMARY: string;
};

export const routes: Routes = {
  HOME: "/",
  PRODUCT: "/product",
  CART: "/cart",
  CHECKOUT: "/checkout",
  ORDER_SUMMARY: "/order-summary",
};

export default routes;
